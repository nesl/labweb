<%= form_for(@document_category) do |f| %>
  <% if @document_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document_category.errors.count, "error") %> prohibited this document_category from being saved:</h2>

      <ul>
      <% @document_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :priority %><br>
    <%= f.number_field :priority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
