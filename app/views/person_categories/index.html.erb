<% provide(:title, 'Person Categories') %>

<div class="panel panel-default lw-content-without-second-nav">
  <!-- Default panel contents -->
  <div class="panel-heading">
    <h1 class="panel-title">Person Categories @ <%= ENV["LABACRONYM"]%></h1>
  </div>
  
  <div class="panel-body">
	  <table class="table-condensed lw-table">
		  <tbody>
			  <% @person_categories.each do |person_category| %>
			    <tr>
					<td><%= person_category.name %></td>
					<td>
						<% if user_signed_in? && current_user.admin? %>
						  &nbsp;&nbsp;&nbsp;&nbsp;[
						  <%= link_to "Details,", person_category %>
						  <%= link_to "Edit,", edit_person_category_path(person_category) %>
						  <%= link_to 'Delete', person_category, method: :delete, data: { confirm: 'Are you sure?' } %>
						  ]
						<% else %>
						  [
						  <%= link_to "Details", person_category %>
						  ]
						<% end %>
				    </td>
			  <% end %>
		  </tbody>
	  </table>
  </div>

   <% if user_signed_in? %>
     <% if current_user.admin? %>
	   <div class="panel-footer">
       <%= link_to 'New Person Category', new_person_category_path %>
	   </div>
     <% end %>
    <% end %>
  
</div>