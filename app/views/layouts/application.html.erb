<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= ENV["META_DESCRIPTION"] %>">
  <meta name="author" content="<%= ENV["META_AUTHOR"] %>">
  <meta name="Keywords" content="<%= ENV["META_KEYWORDS"] %>">
  <link rel="icon" href="/assets/favicon.ico">
  <title><%= full_title(yield(:title)) %></title>
  <link href="//netdna.bootstrapcdn.com/bootstrap/<%= ENV["BOOTSTRAP_VERSION"] %>/css/bootstrap.min.css" rel="stylesheet">
  <link href="/assets/jumbotron.css" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
  <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  <![endif]-->

  <style type="text/css">
      .logo {height:30px}
  </style>

  <a href="#Top"></a>
</head>
<body>

<%= render 'layouts/navigation' %>


<% if current_page?(root_path) %>
    <% if content_for?(:jumbotron) %>
        <div class="jumbotron">
          <div class="container">
            <%= yield :jumbotron %>
          </div>
        </div>
    <% end %>
<% else %>
    <div class="container">
<% end %>

<div class="row">
  <div class="col-md-12">
    <% if flash[:notice] %>
        <div id="notice"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert] %>
        <div id="alert"><%= flash[:alert] %></div>
    <% end %>
  </div>
</div>

<%= yield %>

<footer>
  <hr>
  <p class="pull-right">
    <%= link_to '<span class="glyphicon-class glyphicon glyphicon-chevron-up"></span> Back to top'.html_safe, '#Top' %>
  </p>
  <p>
    &copy; <%= Time.now.year %> <%= ENV["LABACRONYM"] %>
    &bull;
    <%= link_to "Privacy", privacy_path %></li>
    &bull;
    <%= link_to "Terms", terms_path %></li>
  </p>
</footer>
</div> <!-- /container -->


<script src="//netdna.bootstrapcdn.com/bootstrap/<%= ENV["BOOTSTRAP_VERSION"] %>/js/bootstrap.min.js"></script>
</body>

</html>
