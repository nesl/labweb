<%= form_for(@research_area) do |f| %>
  <% if @research_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@research_area.errors.count, "error") %> prohibited this research_area from being saved:</h2>

      <ul>
      <% @research_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table class="table-condensed lw-table">
  <tbody>
  <tr><td>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, size: 100%>
  </div>
  </td></tr>
  <tr><td>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, size: "103x10" %>
  </div>
  </td></tr>
  <tr><td>
  <div class="actions">
    <%= f.submit %>
  </div>
  </td></tr>
  </tbody>
  </table>
<% end %>
