<% provide(:title, 'Product Categories') %>

<div class="panel panel-default lw-content-without-second-nav">
  <!-- Default panel contents -->
  <div class="panel-heading">
    <h1 class="panel-title">Product Categories @ <%= ENV["LABACRONYM"]%></h1>
  </div>
  
  <div class="panel-body">
	  <table class="table-condensed lw-table">
		  <tbody>
			  <% @product_categories.each do |product_category| %>
			    <tr>
					<td><%= product_category.name %></td>
					<td>
						<% if user_signed_in? && current_user.admin? %>
						  &nbsp;&nbsp;&nbsp;&nbsp;[
						  <%= link_to "Details,", product_category %>
						  <%= link_to "Edit,", edit_product_category_path(product_category) %>
						  <%= link_to 'Delete', product_category, method: :delete, data: { confirm: 'Are you sure?' } %>
						  ]
						<% else %>
						  [
						  <%= link_to "Details", product_category %>
						  ]
						<% end %>
				    </td>
			  <% end %>
		  </tbody>
	  </table>
  </div>

   <% if user_signed_in? %>
     <% if current_user.admin? %>
	   <div class="panel-footer">
       <%= link_to 'New Product Category', new_product_category_path %>
	   </div>
     <% end %>
    <% end %>
  
</div>